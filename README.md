# My VSCode + iTerm setup

1. Install Geist Mono font ( otf )
2. Copy and paste the content of `settings.json` ( Need to install all vscode extensions )
3. Load iTerm config
4. Copy and paste zsh config

**Optional** use the snippets
